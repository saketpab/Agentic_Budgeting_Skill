# Agentic_Budgeting_Skill

This repo contains the BudgetBot skill instructions.

Private budgeting history logs should be stored in `private-history/`.
That folder is ignored by git so transaction-level history does not get pushed
to GitHub by default.
